const expect = require('chai').expect

describe('The dummy test module', () => {
    it('it should return 3', () => {
        expect(1+2).to.be.equal(3)
    })
});

describe('Redis basic connection', () => {
    it('it should connect', () => {
        
    })
})